import Link from "next/link";
import { ArrowRight, CheckCircle2, Copy, Sparkles } from "lucide-react";
import { PromptCard } from "@/components/prompt-card";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { getPrompts } from "@/lib/prompts";
import { categoryLabels, promptCategories } from "@/lib/site";
import { getServerTranslator } from "@/i18n/server";

export default async function HomePage() {
  const { locale, t } = await getServerTranslator();
  const { prompts } = await getPrompts({ sort: "popular" });
  const steps = [t("home.step1"), t("home.step2"), t("home.step3"), t("home.step4")];
  return <><section className="border-b bg-[radial-gradient(circle_at_top,rgba(124,92,246,.2),transparent_45%)]"><div className="mx-auto max-w-7xl px-4 py-20 text-center md:py-28"><div className="mx-auto max-w-3xl"><p className="mb-4 inline-flex items-center gap-2 rounded-full border px-3 py-1 text-sm text-violet-600 dark:text-violet-300"><Sparkles className="size-4" />{t("home.eyebrow")}</p><h1 className="text-4xl font-bold tracking-tight md:text-6xl">{t("home.title")}</h1><p className="mx-auto mt-6 max-w-2xl text-lg text-[var(--muted)]">{t("home.description")}</p><div className="mt-8 flex flex-wrap justify-center gap-3"><Button size="lg" asChild><Link href="/generators/crud">{t("home.create")} <ArrowRight className="size-4" /></Link></Button><Button size="lg" variant="outline" asChild><Link href="/prompts">{t("home.explore")}</Link></Button></div></div></div></section><section className="mx-auto max-w-7xl px-4 py-16"><div className="flex items-end justify-between gap-4"><div><p className="text-sm font-semibold text-violet-600 dark:text-violet-300">{t("home.quickEyebrow")}</p><h2 className="mt-2 text-3xl font-bold">{t("home.quickTitle")}</h2><p className="mt-2 text-[var(--muted)]">{t("home.quickDescription")}</p></div><Button variant="outline" className="hidden sm:inline-flex" asChild><Link href="/generators/crud">{t("home.openGenerator")}</Link></Button></div><Card className="mt-7 grid gap-4 p-6 md:grid-cols-4"><div className="md:col-span-3"><p className="font-semibold">{t("home.productManagement")}</p><p className="mt-1 text-sm text-[var(--muted)]">{t("home.quickCard")}</p></div><Button asChild><Link href="/generators/crud">{t("home.start")} <ArrowRight className="size-4" /></Link></Button></Card></section><section className="border-y bg-black/[.02] dark:bg-white/[.02]"><div className="mx-auto max-w-7xl px-4 py-16"><h2 className="text-3xl font-bold">{t("home.categories")}</h2><div className="mt-6 flex flex-wrap gap-3">{promptCategories.map((category) => <Link key={category} href={`/prompts?category=${encodeURIComponent(category)}`} className="rounded-xl border bg-[var(--card)] px-4 py-3 text-sm font-medium hover:border-violet-400">{categoryLabels[locale][category]}</Link>)}</div></div></section><section className="mx-auto max-w-7xl px-4 py-16"><div className="flex items-center justify-between"><h2 className="text-3xl font-bold">{t("home.popular")}</h2><Link className="text-sm font-semibold text-violet-600 dark:text-violet-300" href="/prompts">{t("home.viewAll")}</Link></div>{prompts.length > 0 ? <div className="mt-7 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{prompts.slice(0, 6).map((prompt) => <PromptCard key={prompt.id} prompt={prompt} />)}</div> : <Card className="mt-7 p-8 text-center text-sm text-[var(--muted)]">{t("home.emptyPrompts")}</Card>}</section><section className="border-y"><div className="mx-auto max-w-5xl px-4 py-16"><h2 className="text-center text-3xl font-bold">{t("home.howItWorks")}</h2><div className="mt-8 grid gap-4 md:grid-cols-4">{steps.map((item, index) => <Card className="p-5" key={item}><span className="grid size-8 place-items-center rounded-full bg-violet-600 text-sm font-bold text-white">{index + 1}</span><p className="mt-4 font-semibold">{item}</p></Card>)}</div></div></section><section className="mx-auto max-w-5xl px-4 py-20 text-center"><Copy className="mx-auto size-9 text-violet-500" /><h2 className="mt-4 text-3xl font-bold">{t("home.cta")}</h2><p className="mt-3 text-[var(--muted)]">{t("home.ctaDescription")}</p><Button className="mt-7" size="lg" asChild><Link href="/generators/crud">{t("home.create")} <ArrowRight className="size-4" /></Link></Button></section></>;
}
